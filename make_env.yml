# run `conda env create --file make_env.yml` to create the environment
name: lerner_lab_to_nwb_env
channels:
- conda-forge
- defaults
dependencies:
- python>=3.9
- pip
- pip:
  - -e .  # This calls the setup and therefore requirements minimal
